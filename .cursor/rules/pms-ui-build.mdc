Build frontend UI ONLY for the Hotel PMS Admin.

RULES:
- Frontend only (React + TypeScript).
- No backend logic.
- No Redux, no global stores, no token handling.
- Use mock data placeholders only.
- One component ≤ 250 lines.
- Mobile-friendly (front desk usage).
- Follow existing UI and design patterns.

SCOPE:
- Build ONLY the page or component I request.
- Do NOT create additional pages or flows.
- Do NOT add navigation unless explicitly asked.

Stop after completing the requested UI.
