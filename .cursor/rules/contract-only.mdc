Define API or GraphQL contracts ONLY.

RULES:
- Do NOT write backend implementation code.
- Do NOT write frontend UI code.
- Do NOT create files.
- Do NOT generate .md files.

CONTRACT RULES:
- Backend is the single source of truth.
- Frontend never calculates prices, GST, totals, or availability.
- Include example request and response shapes only.

Output should be schema-level and concise.
