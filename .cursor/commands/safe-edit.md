Modify ONLY the file(s) I explicitly specify.

STRICT RULES:
- Do NOT create new files or folders.
- Do NOT modify any other files.
- Do NOT generate or modify .md files.
- Do NOT add Redux, Zustand, Context, or any global state.
- Do NOT add token handling, JWT logic, or auth flows.
- Do NOT add new dependencies.
- Do NOT refactor unrelated code.

EDITING RULES:
- Keep changes minimal and localized.
- Follow existing project patterns exactly.
- Preserve formatting and structure.
- One component ≤ 250 lines.

If the requested change requires touching another file,
STOP and ask for confirmation before proceeding.
